# ipckecker-2vt
this is build by claude opus 4.6 (RAW AF UNTOUCHABLE)


# 1. Pastikan Python 3.8+ terinstall
python --version

# 2. Buat folder project
mkdir ip-checker-pegadaian
cd ip-checker-pegadaian

# 3. Buat virtual environment
python -m venv venv

# 4. Aktifkan virtual environment
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 5. Install dependencies
pip install -r requirements.txt

# 6. Buat folder yang diperlukan
mkdir -p static/css static/js templates instance

Buka browser → http://127.0.0.1:5000


PUT YOUR API KEY IN .ENV FILE
--
VT_API_KEY=your_actual_virustotal_api_key_here
